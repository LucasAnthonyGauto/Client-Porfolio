import { Input } from '../../Atoms';
import Styles from './styles.module.css'

const Form = () => {
  return (
    <div>
      <Input name={""} onChange={""} placeholder={""} />
      <Input name={""} onChange={""} placeholder={""} />
      <Input name={""} onChange={""} placeholder={""} />
    </div>
  );
};

export default Form;